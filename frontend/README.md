# BasketIQ Frontend

React + Vite frontend for BasketIQ.

## Run

```bash
npm install
npm run dev
```

## Environment

Create `.env` from `.env.example`:

```env
VITE_API_URL=http://localhost:5000/api
```

For full project setup, see the root [README](../README.md).
